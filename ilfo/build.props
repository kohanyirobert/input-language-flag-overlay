<?xml version="1.0" encoding="utf-8"?> 
<Project ToolsVersion="4.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <ImportGroup Label="PropertySheets" />
  <PropertyGroup Label="UserMacros" />
  <PropertyGroup />
  <ItemDefinitionGroup>
    <PostBuildEvent>
      <Command>echo for /f "tokens=* usebackq" %%f in (`where "Unicode $(PlatformArchitecture)-bit.bin"`) do (set bin=%%f)
for /f "tokens=* usebackq" %%f in (`where "Unicode $(PlatformArchitecture)-bit.bin"`) do (set bin=%%f)
if %errorlevel% neq 0 goto exit

echo ahk2exe /in $(ProjectDir)\ilfo.ahk /icon $(ProjectDir)\ilfo.ico /bin "%bin%" /out $(TargetDir)\ilfo.exe
ahk2exe /in $(ProjectDir)\ilfo.ahk /icon $(ProjectDir)\ilfo.ico /bin "%bin%" /out $(TargetDir)\ilfo.exe
if %errorlevel% neq 0 goto exit

echo copy /Y $(ProjectDir)\*.png $(TargetDir)
copy /Y $(ProjectDir)\*.png $(TargetDir)
if %errorlevel% neq 0 goto exit

:exit</Command>
    </PostBuildEvent>
  </ItemDefinitionGroup>
  <ItemGroup />
</Project>
